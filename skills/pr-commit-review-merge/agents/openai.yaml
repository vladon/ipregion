interface:
  display_name: "PR Commit Review Merge"
  short_description: "Commit, review, and merge via PR"
  default_prompt: "Use $pr-commit-review-merge to commit my current changes, open a PR to master, review it, and merge after checks pass."
